#ifndef NUMBERS_H
#define NUMBERS_H
#include "Words.h"
#include <cmath>
#include <string>

int EnterNumber();
int NumberLength(int number);
std::string writeTriplet(int length, int number);
std::string writeText(int number);


#endif