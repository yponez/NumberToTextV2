#ifndef NUMBERS_H
#define NUMBERS_H
#include "Words.h"
#include <cmath>
#include <string>

int EnterNumber();
int NumberLength(int number);
std::string parseTriplet(int length, int number);
std::string parseNumber(int number);


#endif